services:
  campaignx:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      PORT: 3000
      HOSTNAME: 0.0.0.0
      PYTHON_BIN: python3
    ports:
      - "3000:3000"
